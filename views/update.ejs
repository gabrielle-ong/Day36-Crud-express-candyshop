<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Candy App</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
    <link rel="stylesheet" href="stylesheets/main.css"> 
</head>

<body>
    <h1>Candy Shop</h1>
    <hr>
    <div class="container">
        <h2>Update Candy</h2>
        <form method="POST" action=<%= "/candies/" + candy._id + "?_method=PATCH" %> >
            <div class="form-group col-md-4">
              <input name="name" class="form-control" value="<%= candy.name %>"/>
            </div>
            <div class="form-group col-md-4">
              <input name="color"  class="form-control" value="<%= candy.color %>"/>
            </div>
            <div class="form-group col-md-4">
              <button type="submit" class="btn btn-primary col-md-12"> Update </button>
            </div>
        </form>

        <form method="POST" action=<%= "/candies/" + candy._id + "?_method=DELETE" %> >
          <button id="update" class="btn btn-warning col-md-3">Delete</button>
        </form>
    </div>   
</body>
</html>